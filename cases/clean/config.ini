; Inventory tool configuration - non-secret, safe to share.
[general]
app_name = inventory-tool
log_level = info
log_file = app.log

[catalog]
database = catalog.db
reorder_threshold = 15
currency = USD

[backup]
enabled = true
destination = D:\backups
keep_days = 30
