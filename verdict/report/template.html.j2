<!DOCTYPE html>
{# VERDICT report template - filled in by checklist item 9.
   Spec ref: spec.md > Orchestrator > Report generator (report/).
   Must stay a single self-contained file: inline CSS, no JS, anchors only,
   citations jump into the embedded ledger rendering. #}
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>VERDICT report - {{ case_name | default("(case)") }}</title>
  <style>/* TODO(item 9): inline CSS, severity colors, print-friendly layout */</style>
</head>
<body>
  <h1>VERDICT - Investigation Report</h1>
  <p>Template scaffold; the eight report sections are implemented in checklist item 9.</p>
</body>
</html>
